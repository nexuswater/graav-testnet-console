"use client";
import Link from "next/link";
import { useState } from "react";
import { AppChrome } from "@/components/AppChrome";
import { RLUSD_V1 as C } from "@/lib/rlusd-v1/config";
export default function LaunchPage() { const [ticker, setTicker] = useState("MOMENT"); const clone = C.profile === "testnet-clone"; return <AppChrome><main className="g-main" style={{ maxWidth: 620, margin: "0 auto" }}><div className="g-pill" style={{ display: "inline-block" }}>{clone ? "RLUSD TESTNET CLONE · LOCAL MOCK" : "RLUSD COIN V1 · LOCAL ONLY"}</div><h1 className="g-display" style={{ marginTop: 16 }}>Launch a Coin from an X Moment</h1><p className="g-sub" style={{ marginTop: 10 }}>Draft scaffolding for chain target {C.chainId}. V1 is Moment → Coin only; no live factory or posting.</p><div className="g-field" style={{ marginTop: 20 }}><label>Source X post ID</label><input className="sm" defaultValue="demo-moment-2026" /><label style={{ marginTop: 16 }}>Ticker</label><input className="sm" value={ticker} onChange={(e) => setTicker(e.target.value.toUpperCase().replace(/[^A-Z0-9]/g, "").slice(0, 12))} /><div className="g-kv" style={{ marginTop: 18 }}><span>Initial supply</span><span>1,000,000,000</span></div><div className="g-kv"><span>Quote</span><span>{C.quoteSymbol} (MOCK fixture)</span></div><div className="g-kv"><span>Fees</span><span>40 / 35 / 20 / 5 bps</span></div><Link href="/m/demo-moment-2026" className="g-cta" style={{ display: "block", textAlign: "center", textDecoration: "none", marginTop: 20 }}>Review launch draft</Link></div><div className="g-alert" style={{ marginTop: 16 }}>MOCKS are labeled and never count as Squid/provider evidence. Mainnet bind verifying contract remains TBD.</div></main></AppChrome>; }
