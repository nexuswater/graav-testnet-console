import Link from "next/link";
import { AppChrome } from "@/components/AppChrome";
import { MockBuyCard } from "@/components/rlusd/MockBuyCard";
import { mockMarketView } from "@/lib/rlusd-v1/mockMarket";
export const dynamic = "force-dynamic";
export default async function MomentPage({ params }: { params: Promise<{ xPostId: string }> }) { const { xPostId } = await params; const market = mockMarketView(); return <AppChrome><main className="g-main" style={{ maxWidth: 620, margin: "0 auto" }}><Link href="/launch" className="g-micro">← launch draft</Link><div className="g-pill" style={{ display: "inline-block", marginTop: 18 }}>MOMENT → COIN · LOCAL MOCK</div><h1 className="g-display" style={{ marginTop: 14 }}>${xPostId === "demo-moment-2026" ? "MOMENT" : "COIN"}</h1><p className="g-sub" style={{ marginTop: 8 }}>Source post <span className="g-mono">{xPostId}</span> · verified fixture card</p><div className="g-card" style={{ marginTop: 20 }}><div className="g-kv"><span>Chain target</span><span>1440000</span></div><div className="g-kv"><span>Quote</span><span>RLUSD · 18 decimals</span></div><div className="g-kv"><span>Reserve / threshold</span><span>{market.reserve} / {market.threshold} base units</span></div><div className="g-kv"><span>Policy</span><span className="g-mono">{market.policyId}</span></div><div className="g-kv"><span>Graduated</span><span>{String(market.graduated)}</span></div></div><MockBuyCard xPostId={xPostId} /><div className="g-alert" style={{ marginTop: 16 }}>RLUSD V1 is separate from X1: 1449000, native XRP, GRAAVBind, /you and /s remain unchanged.</div></main></AppChrome>; }
